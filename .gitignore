# API keys & secrets — TUYỆT ĐỐI không commit
.env
*.env
secrets.json
api_key.txt
config.py

# Python cache
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.ipynb_checkpoints/

# OS files
.DS_Store
desktop.ini
Thumbs.db

# LaTeX build artifacts — main.pdf IS committed, the intermediates are not
paper/*.aux
paper/*.bbl
paper/*.blg
paper/*.log
paper/*.out
paper/*.toc
paper/*.synctex.gz

# Large files (thêm nếu data > 100MB)
# data/raw/
